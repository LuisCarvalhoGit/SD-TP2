# === SENSOR LOCAL MODE ===
# For local development: connects to Gateway via Docker internal network

ENVIRONMENT=Development
MODE=LOCAL

RABBITMQ_HOST=rabbitmq
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest

# Connect to Gateway via Docker service name
GATEWAY_IP=csharp-gateway
GATEWAY_UDP_PORT=5002
